{
  "name": "ancestry-decoded",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "build:customer": "node scripts/build-report.js",
    "dev": "eleventy --serve",
    "preview": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
